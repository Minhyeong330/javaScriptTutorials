// if I don't need to reassign it -> use const
// If I need to reassign it -> use let

const interestRate = 0.3;
// interestRate = 1; // if replacing const with let, this reassigned value will work
console.log(interestRate);