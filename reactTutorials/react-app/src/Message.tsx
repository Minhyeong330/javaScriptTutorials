// PascalCasing - A convention Capitalize every first letter
function Message() {
    // JSX: JavaScript XML - JSX can easily create dynamic content
    const name = '';
    if (name)
        return <h1>Hello {name}</h1>;
    return <h1>Hello World</h1>;
}

export default Message;