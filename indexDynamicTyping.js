//Two tpyes of programming languages
// 1. Static Language - once declaring the variables, it cannot be changed in the future
// 2. Dynamic Language - The declared varaibles can be changed

let name = 'Minhyeong';
let age = 30; // The type of numbers is only 'number in JS, not float and integer
let isApproved = true;
let firstName = undefined; // undefined is actually a type as well as a value 
let selectedColor = null;
