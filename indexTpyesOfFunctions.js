
//Performing a task
function greet(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
} 

// Calculating a value
function square(number) {
    return number * number
}

let number = square(2); // We need to initialize [square(2) functions] as a variable because the function just returns value
console.log(number);
//or
console.log(square(2))